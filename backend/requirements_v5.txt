# EnricherPro v5.0 Python dependencies
# Install with: pip install -r requirements_v5.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP client
requests>=2.31.0
httpx>=0.27.0

# Pydantic (FastAPI models)
pydantic>=2.6.0

# DNS resolution (Layer 2 validation)
dnspython>=2.6.1

# Environment config
python-dotenv>=1.0.1

# Optional: async support
aiohttp>=3.9.0

# Previously required (keep for backward compat)
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0
